spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:habit_tracker_test;DB_CLOSE_DELAY=-1
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    show-sql: false
    
    
    