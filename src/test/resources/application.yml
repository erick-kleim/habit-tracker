server:
  port: 8888
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false
    show-sql: false
springdoc:
  paths-to-match: /habit/v1/**
  swagger-ui:
    use-root-path: true